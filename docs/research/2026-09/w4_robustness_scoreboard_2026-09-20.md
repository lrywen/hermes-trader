# W4 批次 4-5：稳健性 / 参数敏感性（C/E/F）SCOREBOARD

> 日期：2026-09-20
> 数据：`logs/b3_81coin_filt_ra.jsonl`（B-3，81 币 × 180d × 14 臂，318,195 笔；filt/filt_ra 等权 notional=10000，filt_exch 为 live-sizing 2.08~30）
> 生产配置源：`/data/.agent-config.json`（2026-09-19 拷出，lev10），maxc=2。

---

## 0. 本批决定性结论：结局 B 机械落定

四个对照臂在**每笔等权 bps、按天块自助（5000 次抽天，seed=20260920）**下，
95% CI **全部严格为负、不含 0**：

| arm | 笔数 | 天数 | per-trade 均值 | 按天块自助 95% CI | 裁定 |
|---|---:|---:|---:|---|---|
| filt（对齐实盘出场） | 14288 | 185 | −9.65 bps | **[−13.10, −7.16]** | 显著为负 |
| filt_exch（对齐实盘出场） | 14289 | 185 | −13.00 bps | **[−16.19, −10.27]** | 显著为负 |
| filt_ra（regime-aware，B-3 已否证其出场占比） | 13476 | 185 | −6.66 bps | **[−11.23, −3.69]** | 显著为负 |
| baseline | 35669 | 185 | −12.55 bps | **[−16.76, −11.81]** | 显著为负 |

复现：`.venv/bin/python scripts/bps_block_bootstrap.py`（本批入库）。

**口径修正（重要）**：标准 `backtest_block_bootstrap.py --notional 30` 在 B-3 产物上
会给出 filt −293 vs filt_exch −0.64 的假象——因为它用单一 notional 换算，对已是
live-sizing 的 filt_exch 臂**二次缩放**。故显著性以「每笔自身 notional 算 bps」的
bps 块自助为准（上表），三臂符号一致、结论不依赖量纲。

对照统一文档 §5.2：判据 1–5（W2 已过）+ maxc=2 下净期望**显著为负（CI 不含 0）**
→ **结局 B：回测可信但显示负期望**。按纪律应**停止该 DSL 止损策略的参数优化**，
转向「是否换策略/换周期」决策（对应 C-7），且在出现块自助正面结果前继续禁止离开 SHADOW。

---

## C 类（策略假设）逐项裁定

### 可在现有 180d 数据上判定的

| ID | 假设 | 本批证据 | 裁定 |
|---|---|---|---|
| **C-2** | `protect_pct` 阈值影响「能否进 Phase 2」 | filt/filt_exch 仅 **14.5%** 的信号峰值 ≥1.5%（能进 Phase2 追踪保护）；peak 中位 0.83%、p90 1.74%。baseline 37.3% | **门槛效应量化**：85.5% 的 filt 信号从未进 Phase2，追踪止盈/镜像保护对它们不生效；这与 G2「回吐吃掉毛利」互为因果。是否调阈值需 C-7 周期决策一并评估，单项优化在负期望下无意义 |
| **C-4** | `sl_buffer_bps=10` 是否过紧致交易所抢跑 | filt_exch 中 exchange_trigger 笔（n=1094）净 **+27.23 bps/笔**（中位 +58），优于总体——镜像在快速下挫中锁定更浅出场，**无抢跑损害证据** | **当前无危害证据**；严格的 buffer 多档敏感性（0/5/10/20/40）需参数化重跑 81 币，在结局 B 下不投入，列入换周期后再评估 |
| **C-8** | 154 Stop Market / 19 仓 = 8 次 cancel+replace 是否可优化 | 生产已有 `sl_move.min_interval_sec=30`（每币节流）+ `min_bps=15`（floor 移动 ≥15bps 才重挂）双重护栏（executor.py:858-865, 4935-4945） | **护栏已合理**：8 次/仓是 Phase2 全程棘轮（持仓数小时）在 30s 节流下的结果；进一步减少重挂会放大镜像与 floor 偏离，收益不明，不优化 |

### 时间依赖（现有数据无法闭环，标 BLOCKED，需新月份样本 / 注资）

| ID | 假设 | 阻塞原因 |
|---|---|---|
| **C-1** | 实盘 DSL floor 出场比 filt_ra 更早更浅（原 n=2） | 需更多实盘平仓样本（SHADOW 下成熟 outcome 仍稀疏，受 D-7 回填阻塞） |
| **C-3** | 「6 月 + 8 月依赖」是否随新月份缓解 | 需 9 月之后新月份数据 |
| **C-5** | P3-3「市场级共振」在 maxc=2 下净效应 | B-2 内核已就绪，但共振评分需新样本窗口才有统计力 |
| **C-6** | 「防御性 alpha 无法变现」是否结构性 | 需更多月份区分结构 vs 单 regime |
| **C-7** | 迁 1h 周期是否值得（往返成本占比 195%→61%） | **结局 B 后的主决策方向**；G1 现已完成、可评估，但迁周期引入新变量，应作为下一独立工作项（新 ADR + 独立回测），不在本批混入 |

C 类总评：能判的三项（C-2/C-4/C-8）都**不支持**「靠现有 DSL 参数微调翻盘」；
时间依赖项不阻塞结局 B 判定，留待注资/新样本。

## E 类（配置层）

| ID | 处置 | 落地 |
|---|---|---|
| **E-1** | `candidate_max_loss_pct=1.5` 方向错误（P4 −18.23bps）→ 标注不可转正 | dsl_exit.py `_record_stop_tuning_shadow` 加否证注释，shadow 留痕禁 promote（ab7aa1f） |
| **E-2** | `candidate_breakeven_trigger_pct=1.0` 把追踪止盈 +218→+73bps → 标注不可转正 | 同上（ab7aa1f） |
| **E-3** | `regime_risk_overlay.shadow_mode` 转正门槛 | **BLOCKED：依赖 D-3，而 D-3 被 D-7 outcome 回填阻塞**；转正前须有块自助正面结果（本批已证当前 DSL 为负，更不具备转正条件） |
| **E-4** | xs_reversal 用 `mode` 无 `enabled` 键 | 论证三态 mode（off/shadow/enforce + env fail-closed）是 enabled+shadow_mode 的严格超集，认定 mode 为权威键，config_store 加规范注释，不引入冗余键（ab7aa1f） |

## F 类（流程）

| ID | 处置 | 落地 |
|---|---|---|
| **F-2** | 回测数字必标「配置源 + 币池 + maxc」 | 持久规范 `docs/backtest-number-conventions.md`（权威全文指统一文档附录 D）（ab7aa1f） |
| **F-3** | 配置变更必记 ADR | 持久 `docs/adr/`：README 流程 + `0000-template` + `0001` 据丢失原件诚实重建三不变量（ab7aa1f） |

---

## 里程碑

- **M2→W4 出口达成**：块自助正面/负面结论落定＝**负面（结局 B）**，CI 不跨 0。
- 不改动任何生产交易语义；本批新增 1 个标准件脚本（bps 块自助）+ 注释/文档。
- 下一决策点（交用户）：结局 B 下，是否启动 **C-7（换 1h 周期）/ 换策略** 的独立评估，
  或走路径 B 注资 $1k–5k 积累样本。两者均需新 ADR，且在此之前保持 SHADOW。
